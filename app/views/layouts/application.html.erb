<!DOCTYPE html>
<html>
  <head>
    <title>Ari1App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header">
      <%= link_to "Question AriApp", root_path, class: "hptitle" %>
      <div class="header_right">
        <% if user_signed_in? %>
          <%= link_to current_user.nickname + "さんのマイページ", "#", class: :header_btn %>
          <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: :header_btn %>
        <% else %>
          <%= link_to "ログイン", new_user_session_path, class: :header_btn %>
          <%= link_to "新規登録", new_user_registration_path, class: :header_btn %>
        <% end %>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright © AriApp All rights reserved.</p>
    </footer>
  </body>
</html>
